<%= form_with(model: abastecimiento_obra, local: true) do |form| %>
  <% if abastecimiento_obra.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(abastecimiento_obra.errors.count, "error") %> prohibited this abastecimiento_obra from being saved:</h2>

      <ul>
      <% abastecimiento_obra.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
