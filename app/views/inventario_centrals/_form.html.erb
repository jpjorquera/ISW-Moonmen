<%= form_with(model: inventario_central, local: true) do |form| %>
  <% if inventario_central.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(inventario_central.errors.count, "error") %> prohibited this inventario_central from being saved:</h2>

      <ul>
      <% inventario_central.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
