<%= form_with(model: abastecimiento_central, local: true) do |form| %>
  <% if abastecimiento_central.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(abastecimiento_central.errors.count, "error") %> prohibited this abastecimiento_central from being saved:</h2>

      <ul>
      <% abastecimiento_central.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
