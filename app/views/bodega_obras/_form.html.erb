<%= form_with(model: bodega_obra, local: true) do |form| %>
  <% if bodega_obra.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bodega_obra.errors.count, "error") %> prohibited this bodega_obra from being saved:</h2>

      <ul>
      <% bodega_obra.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
