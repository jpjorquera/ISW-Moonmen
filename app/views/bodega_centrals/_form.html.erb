<%= form_with(model: bodega_central, local: true) do |form| %>
  <% if bodega_central.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bodega_central.errors.count, "error") %> prohibited this bodega_central from being saved:</h2>

      <ul>
      <% bodega_central.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
