<%= form_with(model: bodeguero_central, local: true) do |form| %>
  <% if bodeguero_central.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bodeguero_central.errors.count, "error") %> prohibited this bodeguero_central from being saved:</h2>

      <ul>
      <% bodeguero_central.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
